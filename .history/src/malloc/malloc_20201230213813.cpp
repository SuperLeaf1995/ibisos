#include <malloc/malloc.h>

void *malloc(size_t size)
{
    //Check if the size exceeds maximum allocatable memory
    if(size <= MAX_MEMORY_SIZE)
    {
        memory_block* current_block = (memory_block*) sizel
        current_block->
    }
}